const message = () => {
  const name = "Aishwarya K";
  const age = 20
  const profile = "Java Developer"
  return name + ' is ' + age + 'years old.' + profile + 'is my profession';
};

export default message;
